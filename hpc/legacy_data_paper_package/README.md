# Legacy HPC helpers

This directory preserves older HPC helper scripts for the previous
Data-Paper-style ScienceDB archive and H5AD conversion workflow.

The active HPC entry points are now:

```sh
sbatch hpc/hpc_integration.sbatch
sbatch hpc/hpc_metadata.sbatch
sbatch hpc/hpc_sciencedb.sbatch
```

The active compact package output is:

```text
/path/to/hpc/home/data/BrainOmicsData/ScienceDB
```
